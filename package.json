{
  "scripts": {
    "precommit": "cd native && npm run precommit",
    "prepare": "husky install",
    "setup:app": "cd native && npm install && cd ..",
    "setup:landing": "cd landing && npm install && cd ..",
    "setup:admin": "cd admin && npm install && cd ..",
    "setup": "npm run setup:app ; npm run setup:landing ; npm run setup:admin"
  },
  "devDependencies": {
    "husky": "^8.0.3"
  },
  "dependencies": {
    "react-native-dotenv": "^3.4.8"
  }
}
