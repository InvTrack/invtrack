{
  "scripts": {
    "precommit": "cd native && npm run precommit",
    "prepare": "husky install",
    "setup:app": "cd native && npm install",
    "setup:landing": "cd landing && npm install",
    "setup:admin": "cd admin && npm install",
    "setup": "npm run setup:app ; npm run setup:landing ; npm run setup:admin",
    "gen-types": "supabase gen types typescript --local > database.types.ts && cd native && npm run gen-types && cd ../admin && npm run gen-types",
    "start": "supabase start",
    "stop": "supabase stop",
    "reset-db": "supabase db reset",
    "status": "supabase status"
  },
  "devDependencies": {
    "husky": "^8.0.3"
  },
  "dependencies": {
    "react-native-dotenv": "^3.4.8",
    "supabase": "^1.110.1"
  }
}
